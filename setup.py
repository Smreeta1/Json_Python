from setuptools import setup, find_packages #  find_packages:find and list all python packages __init__.py files

setup(
    name="Json",
    author="Smreeta",
    version="1.0",
    packages=find_packages(),
    install_requires=[], 
    description="JSON data in python",
    
)